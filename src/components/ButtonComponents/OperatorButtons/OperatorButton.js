import React from "react";

const OperatorButton = (props) => {
  return (
    // <div className="layout2">
    <button className="operators">
      {/* Display a button element rendering the data being passed down from the parent container on props */props.operators}
    </button>
    // </div>
  );
};

export default OperatorButton;